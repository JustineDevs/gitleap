# OpenTUI

This terminal UI app uses OpenTUI's core renderer.

## Scripts

- `bun run dev` starts the TUI with Bun.
- `bun run check-types` type-checks the TUI source.

Press `Ctrl+C` to exit the running TUI.
